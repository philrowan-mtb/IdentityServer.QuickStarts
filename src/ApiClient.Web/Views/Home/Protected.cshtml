@{
    ViewData["Title"] = "Info";
}
<dl>
    @foreach (var claim in User.Claims)
    {
        <dt>@claim.Type</dt>
        <dd>@claim.Value</dd>
    }
</dl>

<div>
    <a asp-action="Logout">Logout</a>
</div>